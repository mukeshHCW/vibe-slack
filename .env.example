# Environment Configuration for EEG Vibe Slack

# Development URLs
DEV_CLIENT_URL=http://localhost:3000
DEV_SERVER_URL=http://localhost:5000

# Production URLs (Update these for your deployment)
PROD_DOMAIN=eeg-vibe-slack.eastus.cloudapp.azure.com
PROD_CLIENT_PORT=3000
PROD_SERVER_PORT=5000
PROD_VM_IP=4.157.242.240

# Derived URLs (do not modify these)
PROD_CLIENT_URL=http://${PROD_DOMAIN}:${PROD_CLIENT_PORT}
PROD_SERVER_URL=http://${PROD_DOMAIN}:${PROD_SERVER_PORT}
PROD_CLIENT_IP_URL=http://${PROD_VM_IP}:${PROD_CLIENT_PORT}
PROD_SERVER_IP_URL=http://${PROD_VM_IP}:${PROD_SERVER_PORT}

# Azure Configuration
AZURE_RESOURCE_GROUP=vibe-slack-rg
AZURE_VM_NAME=vibe-slack-vm
AZURE_NSG_NAME=vibe-slack-vmNSG

# Security
JWT_SECRET=your-secret-key-change-in-production-please-use-strong-key
